var Slots = (function() {
    //the main slot class
    return {
        init: function() {
            //initialize all stuff
            console.info('game initalized!');
        }
    };
})();

Slots.init();